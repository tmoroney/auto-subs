{
  "name": "autosubs-telemetry-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that ingests AutoSubs anonymous usage summaries",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.90.0"
  }
}
